# CLAUDE.md

このファイルは、このリポジトリで Claude Code (claude.ai/code) が作業する際のガイドラインを記載したものです。

## プロジェクト概要

`real-estate` プロジェクト。現時点ではプロジェクトは初期状態です。開発が進み次第、
このセクションに以下の情報を追記してください。

- プロジェクトの目的・概要
- 主要な技術スタック（言語・フレームワーク・DB など）
- ディレクトリ構成
- セットアップ・実行方法（`npm install` / `npm run dev` など）
- テスト・Lint の実行コマンド

## Git 運用ルール

**コードに変更を加えるたびに、コミットして GitHub にプッシュすること。**

- 作業を区切りの良い単位（1つの修正・1つの機能追加など）に分け、変更のたびに
  コミット → プッシュ まで完了させる。変更を溜め込んでまとめてプッシュしない。
- コミットメッセージは変更内容が分かるように簡潔に記載する。
- push 先のリモート・ブランチが未設定の場合は、作業前にユーザーに確認する。
- `main` / `master` などのデフォルトブランチで直接作業している場合、変更前に
  作業用ブランチを切ってからコミットする。
- 破壊的な操作（force push、履歴の書き換えなど）は、明示的な指示がない限り行わない。
- コミット前に差分の内容を確認し、意図しないファイル（機密情報・ビルド成果物など）
  が含まれていないことを確認する。

### 基本フロー

```bash
git add <変更したファイル>
git commit -m "変更内容が分かるメッセージ"
git push
```

## 今後の追記事項

プロジェクトが具体化してきたら、以下も追記してください。

- コーディング規約
- ディレクトリ／モジュール構成のルール
- 環境変数・秘匿情報の扱い方
- デプロイ手順
